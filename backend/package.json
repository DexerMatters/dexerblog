{
  "name": "dexerblog-backend",
  "version": "0.1.0",
  "scripts": {
    "dev": "npx tsc --watch & node --watch dist/server.js",
    "build": "npx tsc",
    "start": "npx tsc && node dist/server.js"
  },
  "dependencies": {
    "@types/express": "^5.0.3",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}